@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

@model HomePageViewModel

<div class="container mt-4">
    <h2 class="text-white">Spaceships</h2>
    <div id="spaceshipsCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            @for (int i = 0; i < Model.Spaceships.Count(); i++)
            {
                var spaceship = Model.Spaceships.ElementAt(i);
                <div class="carousel-item @(i == 0 ? "active" : "")">
                    <div class="card text-center mx-auto" style="max-width: 300px;">
                        <div class="card-body">
                            <h5 class="card-title">@spaceship.Name</h5>
                            <p class="card-text">@spaceship.SpaceshipModel</p>
                        </div>
                    </div>
                </div>
            }
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#spaceshipsCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#spaceshipsCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>

    <h2 class="text-white mt-4">Kindergartens</h2>
    <div id="kindergartensCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            @for (int i = 0; i < Model.Kindergartens.Count(); i++)
            {
                var kindergarten = Model.Kindergartens.ElementAt(i);
                <div class="carousel-item @(i == 0 ? "active" : "")">
                    <div class="card text-center mx-auto" style="max-width: 300px;">
                        <div class="card-body">
                            <h5 class="card-title">@kindergarten.KindergartenName</h5>
                            <p class="card-text">@kindergarten.GroupName</p>
                        </div>
                    </div>
                </div>
            }
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#kindergartensCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#kindergartensCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>

    <h2 class="text-white mt-4">Real Estates</h2>
    <div id="realEstatesCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            @for (int i = 0; i < Model.RealEstates.Count(); i++)
            {
                var realEstate = Model.RealEstates.ElementAt(i);
                <div class="carousel-item @(i == 0 ? "active" : "")">
                    <div class="card text-center mx-auto" style="max-width: 300px;">
                        <div class="card-body">
                            <h5 class="card-title">@realEstate.Size</h5>
                            <p class="card-text">@realEstate.Location</p>
                        </div>
                    </div>
                </div>
            }
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#realEstatesCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#realEstatesCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
        </button>
    </div>
</div>



